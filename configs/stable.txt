# Physical parameters
Re = 100.0                 # Reynolds number (low value for stability)
Lx = 1                     # Length
Ly = 1                     # Width

# Numerical parameters
nx = 32                    # Grid resolution in x direction (low for stability)
ny = 32                    # Grid resolution in y direction (low for stability)
dt = 0.0001                  # Time step (very small for stability)
tf = 50.0                   # Final time (shorter for testing)
max_co = 1.0               # Max Courant number
order = 2                  # Finite difference order (lower for stability)
poisson_max_it = 50000     # Poisson equation max iterations (very high)
poisson_tol = 0.01         # Poisson equation tolerance (very relaxed)
output_interval = 5        # Output interval for VTK files
poisson_type = 2           # 1 - no relaxation | 2 - successive overrelaxation

# Acceleration options
use_omp = 1                # Use OpenMP for parallelization
use_gpu = 1                # Use GPU for Poisson solver
use_vulkan = 0             # Use Vulkan for Poisson solver

# Object parameters
center_x = -1.0             # Center of object in x direction
center_y = -1.0             # Center of object in y direction
radius = 0.01               # Radius of circular object 