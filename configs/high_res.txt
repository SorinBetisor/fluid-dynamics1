# Physical parameters
Re = 100.0                 # Reynolds number (low for better stability)
Lx = 1                     # Length
Ly = 1                     # Width

# Numerical parameters
nx = 128                   # Grid resolution in x direction (high for better visualization)
ny = 128                   # Grid resolution in y direction (high for better visualization)
dt = 0.005                 # Time step (small for stability)
tf = 10.0                  # Final time (longer for more developed flow)
max_co = 1.0               # Max Courant number
order = 4                  # Finite difference order
poisson_max_it = 10000     # Poisson equation max iterations
poisson_tol = 0.01         # Poisson equation tolerance (relaxed for stability)
output_interval = 20       # Output interval for VTK files (less frequent for fewer files)
poisson_type = 2           # 1 - no relaxation | 2 - successive overrelaxation

# Acceleration options
use_omp = 1                # Use OpenMP for parallelization
use_gpu = 1                # Use GPU for Poisson solver
use_vulkan = 0             # Use Vulkan for Poisson solver

# Object parameters
center_x = 0.5             # Center of object in x direction
center_y = 0.5             # Center of object in y direction
radius = 0.1               # Radius of circular object 